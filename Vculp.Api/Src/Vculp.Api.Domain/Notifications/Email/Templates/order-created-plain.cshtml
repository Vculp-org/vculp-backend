@using Vculp.Api.Domain.Notifications
@model Vculp.Api.Common.Notifications.Commands.SendOrderCreatedEmailToCustomerCommand;

Your order (attached) has been queued for production.

@{
    await IncludeAsync(NotificationConstants.FooterTemplateTextPlain);
}